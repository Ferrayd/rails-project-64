meta:
  environment: ruby
  toolchain:
    name: bundle
    version: 3.2.2
build:
  image: null
run:
  image: null
  mainScript: /bin/render-build.sh
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
